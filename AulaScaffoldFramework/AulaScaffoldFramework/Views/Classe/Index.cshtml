@model IEnumerable<AulaScaffoldFramework.Models.CLASSE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_SUB_GRU)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOME_CLA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_SUB_GRU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOME_CLA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_CLAS }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_CLAS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_CLAS })
        </td>
    </tr>
}

</table>
