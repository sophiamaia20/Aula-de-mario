@model IEnumerable<AulaScaffoldFramework.Models.UNIDADE_MEDIDA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOME_UN_MED)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SIGLA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOME_UN_MED)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SIGLA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_UN_MED }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_UN_MED }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_UN_MED })
        </td>
    </tr>
}

</table>
