@model IEnumerable<AulaScaffoldFramework.Models.GRUPO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOME_GRU)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SUGESTAO_GRU)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOME_GRU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SUGESTAO_GRU)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_GRU }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_GRU }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_GRU })
        </td>
    </tr>
}

</table>
