@model IEnumerable<AulaScaffoldFramework.Models.SETOR>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_SEC)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOME_SET)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_SEC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOME_SET)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_SET }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_SET }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_SET })
        </td>
    </tr>
}

</table>
